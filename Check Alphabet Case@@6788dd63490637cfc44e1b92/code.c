#include<stdio.h>

int main(){
    char ch;
    if (ch >= 'A' && ch <='Z'){
        printf("Uppercase\n");
    }
    else if(ch >= 'a' &7 ch<='z'){
        printf("Lowercase\n");
    }
    else(
        printf("Not an alphabet");
    )
    return 0;
}